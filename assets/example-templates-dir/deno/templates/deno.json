{
  "tasks": {
    "build": "deno compile --output=./runserver --import-map=./import_map.json --allow-net --allow-run=./runserver --allow-env --allow-read --unstable-sloppy-imports src/main.ts",
    "start": "deno run --import-map=./import_map.json --allow-net --allow-run=./runserver --allow-env --allow-read --unstable-sloppy-imports src/main.ts",
    "dev": "deno run --import-map=./import_map.json --allow-net --allow-run=./runserver --allow-env --allow-read --unstable-sloppy-imports --watch=src src/main.ts",
    "format": "deno fmt"
  }
}
